build:
  docker:
    web: Dockerfile

run:
  web: node index.js

config:
  - NODE_ENV: "production"
  - PORT: 80
  - WHATSAPP_SESSION_NAME: "your-session-name"
  - WHATSAPP_API_KEY: "your-api-key-here"